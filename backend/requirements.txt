# Core Backend
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
python-dotenv==1.2.3

# OpenAI
openai==1.109.1

# The live research pipeline (agent/research_pipeline.py) calls the Tavily
# HTTP API directly via httpx, not through the tavily-python SDK. LangChain,
# tavily-python, beautifulsoup4, requests and lxml were only used by the
# LangChain ReAct agent + scraper path, which has been removed (it held an
# unmitigated SSRF — see agent/research_agent.py / tools/scraper.py in git
# history) and was never wired into any live endpoint. httpx is pulled in
# transitively by openai/supabase.

# Token tracking
tiktoken==0.14.0


# Database
supabase==2.31.0
websockets==15.0.1

# Rate limiting is hand-rolled in utils/rate_limit.py — no dependency needed.
# slowapi was dropped: its engine (`limits`) ships only fixed-window,
# moving-window and sliding-window-counter strategies, none of which give the
# burst-then-drip behaviour this app's clustered traffic needs, and its
# key_func only receives the Request, so it cannot key on the authenticated
# user id (which lives in a dependency's return value).